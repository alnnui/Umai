# 🔐 Система авторизации и форм Umai Nury

## ✅ Что было создано

### Note: Database integration removed

This repository no longer includes a server-side database integration by default.
Forms now send JSON payloads to the local Express endpoint `POST /notify`, which
forwards formatted messages to the configured Telegram chat. If you need the
previous server-side workflow (schema, policies, or client usage), restore the
files from version control history or contact the maintainers.

#### Components (forms)

```
src/components/Forms/VolunteerForm.tsx     - Form (POST → /notify)
src/components/Forms/PsychologistForm.tsx  - Form (POST → /notify)
src/components/Forms/ChapterForm.tsx       - Form (POST → /notify)
src/components/Forms/PartnerForm.tsx       - Form (POST → /notify)
```

#### Features:

- ✅ Field validation
- ✅ POST to `/notify` (Telegram notifications)
- ✅ Success/Error messages
- ✅ Works without auth
- ✅ Multi-language support (EN/RU/KK)

### Psychologist Application (Заявка психолога)

```typescript
{
  id: string;
  user_id?: string;
  full_name: string;
  email: string;
  phone: string;
  city: string;
  specialization: string;     // Специализация
  experience_years: number;   // Годы опыта
  license_number: string;     // Номер лицензии
  about: string;              // О себе
  status: 'pending' | 'approved' | 'rejected';
  created_at: string;
}
```

### Chapter Application (Заявка на филиал)

```typescript
{
  id: string;
  user_id?: string;
  full_name: string;
  email: string;
  phone: string;
  city: string;
  institution: string;        // Учреждение/организация
  proposal: string;           // Предложение
  team_size?: number;         // Размер команды (опционально)
  status: 'pending' | 'approved' | 'rejected';
  created_at: string;
}
```

### Partner Inquiry (Запрос партнёра)

```typescript
{
  id: string;
  user_id?: string;
  company_name: string;       // Название компании
  contact_name: string;       // Контактное лицо
  email: string;
  phone: string;
  company_type: string;       // Тип компании
  message: string;            // Сообщение
  status: 'pending' | 'contacted' | 'completed';
  created_at: string;
}
```

---

## 🎯 Примеры использования (локально)

Примеры работы в этой ветке предполагают отправку формы на локальный
endpoint `/notify`. Сервер форматирует сообщение и отправляет его в Telegram.

### Получить все заявки (если реализован бэкенд)

Реализация хранения/запроса данных не включена в текущую ветку. Если вам
нужна серверная логика для хранения заявок, создайте API-эндпоинт на сервере
и реализуйте чтение из вашей БД (Postgres, MySQL и т.д.).

---

## 🔒 Безопасность (заметка)

Если вы добавите серверную базу данных, следуйте лучшим практикам безопасности:

1. Ограничьте права доступа через RLS / ACLs
2. Используйте секреты (не коммитьте их в репозиторий)
3. Для административных задач используйте защищённые сервисные ключи

---

## 🌍 Многоязычность

Все формы и auth компоненты поддерживают 3 языка:

- 🇬🇧 English (en)
- 🇷🇺 Русский (ru)
- 🇰🇿 Қазақша (kk)

Переводы находятся в `src/lib/translations.ts`

---

## ✨ Дополнительные возможности

### Что можно добавить:

1. **Email уведомления** - При новой заявке
2. **Admin панель** - Для управления заявками
3. **Dashboard** - Статистика и аналитика
4. **Экспорт данных** - В Excel/CSV
5. **Фильтры и поиск** - По всем полям
6. **История изменений** - Audit log

---

## 🚀 Запуск проекта

```bash
# Установка зависимостей
npm install

# Запуск dev сервера (автоматически)
npm run dev

# Сборка проекта
npm run build
```

---

## 📝 Важные заметки

1. **Миграция не включена в этой ветке** - Таблицы не создаются автоматически
2. **RLS включен** - Данные защищены
3. **Формы работают** - Можно принимать заявки
4. **Auth настроен** - Регистрация/вход работают
5. **Переводы добавлены** - Поддержка 3 языков (частично, можно дополнить)

---

## 🐛 Что нужно проверить

- [ ] Email подтверждение настроено (если требуется)
- [ ] Настройки SMTP для email уведомлений
- [ ] Переводы на RU и KK для auth и форм (частично добавлены)
- [ ] Тестирование всех форм на разных устройствах
- [ ] Настройка редиректов после регистрации

---

## 📞 Поддержка

Если возникнут вопросы:

1. Проверьте `.env` файл
2. Проверьте Dashboard вашей базы данных или инфраструктуру
3. Посмотрите browser console на ошибки
4. Проверьте Network tab в DevTools

---

**Проект готов к использованию! 🎉**
